

<h1 class="enroll-heading">Enroll in <%= @program.name %></h1>

<% if current_user.students.any? %>
  <div class="student">
    <button><%= link_to "Add an existing student", new_enrollment_path(program_id: @program.id, existing_student: true) %></button>
  
  <h4>OR</h3>
  
    <button><%= link_to "Create a new student", new_student_path %></button>
  </div>
<% else %>
  <div>
    <%= link_to "Create a new student", new_student_path %>
  </div>
<% end %>
