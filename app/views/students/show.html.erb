<div class="container">
    <div class="col-md-4 offset-md-4">
        <h3> <%= @student.full_name %> </h3>
        <p id="dob-box"> <%= @student.dob.strftime('%m/%d/%Y') %> </p>
        <p id="enrollment-box"> Enrolled in: <%= display_enrolled_programs(@student) %> </p>
    </div>
</div>
