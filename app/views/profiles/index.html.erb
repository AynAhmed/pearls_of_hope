
<h1>Profiles</h1>
<ul>
  <% @profiles.each do |profile| %>
    <li><%= button_to "#{profile.first_name} #{profile.last_name}", profile_path(profile), method: :get, class: 'profile-button' %></li>
  <% end %>
</ul>
