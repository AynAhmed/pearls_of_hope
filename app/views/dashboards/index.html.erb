<%# app/views/dashboards/show.html.erb %>

<% if @user.role == 'instructor' %>
  <%= render 'instructor_dashboard', programs: @programs, students: @students, user: @user %>
<% else %>
  <%= render 'default_dashboard', programs: @programs, students: @students, user: @user %>
<% end %>
